[project]
name = "vcc-project-carbon-aware-vm-migration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "pytest>=9.0.2",
    "requests>=2.31.0",
    "numpy>=1.26.0",
    "torch>=2.0.0",
]
